export const PROVIDER_DOMAIN_LABEL_OVERRIDES: Record<string, string> = {
  "kleomedes.network": "Kleomedes"
};

export const SUPPLIER_PROVIDER_OVERRIDES: Record<
  string,
  {
    providerKey: string;
    providerLabel?: string;
    providerDomain?: string;
  }
> = {};
